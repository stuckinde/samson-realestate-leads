services:
  - type: web
    name: samson-realestate-leads
    env: python
    buildCommand: pip install -r server/requirements.txt
    startCommand: uvicorn server.app:app --host 0.0.0.0 --port $PORT
    plan: free
